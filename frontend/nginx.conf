# Serve the React SPA. Any unknown path falls back to index.html so client-side
# routing (react-router) works on refresh/deep links.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
