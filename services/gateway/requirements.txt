# gateway deps. Installs documind-contracts + documind-common (the latter only
# for shared structlog logging config) in the Dockerfile.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35

pydantic>=2.9,<3
pydantic-settings>=2.6,<3

httpx>=0.27                       # async reverse proxy + SSE streaming
PyJWT>=2.9                        # JWT mint/verify
bcrypt>=4.2                       # password hashing
redis>=5.1                        # async, shared rate-limit counter

SQLAlchemy>=2.0,<3
asyncpg>=0.30                     # users table

structlog>=24.4

# Tests
pytest>=8.3
