# Service-specific deps. The shared libs (documind-contracts, documind-common)
# are installed separately in the Dockerfile and pull in langchain, the pgvector
# store, and the OpenAI client.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35

pydantic>=2.9,<3
pydantic-settings>=2.6,<3

motor>=3.6                        # async MongoDB driver (conversation history)

langfuse>=2.0,<3                  # LLM observability (no-op unless keys are set)

# Enabling the cross-encoder reranker (RERANKER=cross-encoder) additionally needs:
#   sentence-transformers>=3.0     # pulls torch (~2GB) — left out of the default image

# Tests
pytest>=8.3
pytest-asyncio>=0.24
