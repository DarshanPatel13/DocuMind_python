# Web framework
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35
python-multipart>=0.0.12          # multipart/form-data parsing for file uploads

# Models & config
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# RAG orchestration
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-postgres>=0.0.12        # PGVector vector store integration
langchain-text-splitters>=0.3,<0.4
openai>=1.54

# To switch the CHAT model to Anthropic Claude later, also install:
# langchain-anthropic>=0.2

# Postgres (metadata via SQLAlchemy async; vectors via PGVector/psycopg)
SQLAlchemy>=2.0,<3
asyncpg>=0.30                      # async driver for SQLAlchemy metadata engine
psycopg[binary]>=3.2              # driver PGVector uses for the vector store

# MongoDB (conversation history)
motor>=3.6

# Kafka (async ingestion)
aiokafka>=0.12

# PDF extraction
pypdf>=5.1

# Cross-cutting
slowapi>=0.1.9                     # rate limiting
structlog>=24.4                    # structured logging

# Tests
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27                        # FastAPI TestClient transport
